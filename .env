# Variables prefixed with VITE_ are exposed to browser client
# Overrides can be generated by creating a .env.local file with the variables to override (same syntax as this file)
#   modifying this file directly is discouraged as it can cause merge conflicts when there are updates
#   see https://vitejs.dev/guide/env-and-mode.html#env-files for details

VITE_API_URL = https://hvdata.gisadan.dev